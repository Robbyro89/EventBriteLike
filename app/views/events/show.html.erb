<h1 style="color: #FF8C00"><b>Regarde un peu cet Evénement de dingue !!!</b></h1>

<div class="center jumbotron" style="color: #FFFFFF">
  <h2><b>Evénement :</b> <i>#<%= @event.id %></i></h2>
  <h4><b>Organisateur :</b> <i><%= @event.creator.name %></i></h4>
  <h4><b>Lieu de l'événement :</b> <i><%= @event.place %></i></h4>
  <h4><b>Date de l'événement :</b> <i><%= @event.date.strftime("%d %B %Y") %></i></h4>
  <h4><b>description de l'événement :</b></h4>
  <h6><li><i><%= @event.description %></i></li></h6>
</div>

<div class="center jumbotron" style="color: #FFFFFF">
  <h3><b>Wouaaah ! Tous les followers!!!</b></h3>
  <br>
  <% if !@event.attendees.any? %>
    <div><i>Aucun participant</i></div>
  <% else %>
    <table class="table table-bordered"  style="color: #FFFFFF">
      <thead>
        <tr>
          <th><b>Name</b></th>
          <th><b>Email</b></th>
        </tr>
      </thead>

      <tbody>
        <% @event.attendees.each do |attendee| %>
          <tr>
            <td><%= attendee.name%></td>
            <td><%= attendee.email %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  <% end %>
</div>
