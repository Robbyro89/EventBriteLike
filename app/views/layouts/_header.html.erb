<nav class="navbar navbar-expand-sm navbar-light" style="background-color: #FF8C00" id="navheader">
  <div class="container-fluid">
    <a class="navbar-brand" href="/" id="brand"><b>EventbriteLike</b></a>
      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%=current_user.name %>
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Mon Compte", current_user %></li>
              <li class="divider"></li>
              <li>
                <%= link_to "Log out", logout_path, method: :delete %>
              </li>
            </ul>
          </li>
        <% else %>
          <li>
            <a href="#" class="btn btn-outline" role="button" id="sign">S'inscrire</a>
          </li>
          <li>
            <a href="#" class="btn btn-outline" role="button" id="log">Se connecter</a>
          </li>
        <% end %>
      </ul>
  </div>
</nav>
